---
description: Runs the architect→planner→coder|coder-web→qa pipeline for a task in api-core (or web-app), then integration tests + adversary + security-review in parallel, with bounded QA and adversary retry loops.
argument-hint: [--en .worktrees/<repo>--<slug>] <descripción de la tarea o feature a implementar>
---

You are acting as the Orchestrator for the api-core multi-agent pipeline. The task is: $ARGUMENTS

**Target repo**: this command lives in `AcmeOrg/.claude/` (shared across `api-core`, `api-contracts`, `api-people`), not inside any single repo. Unless the task explicitly names a different target repo, **default target is `api-core`**. All `git status`/`git checkout -b`/`dotnet build`/`dotnet test` operations run with working directory set to `api-core/` (a sibling folder of this command's location), not the AcmeOrg root. Every relative path below (`api-core/...`) is relative to the target repo's root (`api-core/`), already written with that prefix. **Excepción: los run docs y los ADRs** — `docs/runs/` (con `INDEX.md`, `RETRO-LOG.md`, `GOLDEN-TASKS.md` y `SYSTEM-AUDIT-LOG.md`) y `docs/adr/` viven en la raíz de AcmeOrg (repo `agentic-harness`), no en el repo destino *(movidos el 2026-09-16)*.

**Destino en worktree (`--en <ruta>`)** *(added 2026-09-11, trabajo por olas — ver `/worktree`)*: si `$ARGUMENTS` empieza con `--en .worktrees/<repo>--<slug>`, esa carpeta es la **raíz de trabajo** (`<RAIZ>`) de toda la corrida y el repo destino es el `<repo>` de su nombre. Reemplaza a la carpeta principal del repo en **todo**: `git`, `dotnet build`/`test`, `npm` y el diff de los gates del Step 4.4. Lee cada ruta `api-core/...` de este archivo como `<RAIZ>/...`. Los repos de referencia (`api-contracts/`, `api-people/`) se siguen leyendo donde están. Los run docs y los ADRs tampoco siguen a `<RAIZ>`: siempre van a `docs/runs/` y `docs/adr/`, una sola copia para todos los worktrees. Sin `--en`, nada de esto aplica: `<RAIZ>` es la carpeta del repo destino, como siempre.

Los agentes no lo saben si no se lo dices — sus `.md` nombran `api-core/` a secas. Con `--en`, **cada** prompt a `architect`, `planner`, `coder`, `coder-web`, `qa` y `adversary` empieza con esta línea, literal: `Raíz de trabajo: <RAIZ> (rama <rama>). Todo archivo del repo destino que leas, edites, compiles o diffees va bajo esa ruta; la carpeta principal del repo no se toca.`

Con `--en` **no se sube la rama ni se abre PR, y tampoco se ofrece** (pedido del responsable del repo, 2026-09-11): la corrida termina en el commit del Step 4.5 más el Step 5. La rama pasa por `/worktree sincronizar` y el push y el PR se hacen solo cuando el responsable del repo los pide.

Follow this sequence exactly. Do not skip steps, do not let two agents talk to each other directly — every hand-off goes through you.

**Structured hand-offs**: each agent outputs a fixed markdown structure — `## Architect Report` (`architect`), `## Plan` (`planner`), `## Coder Report` (`coder` **and** `coder-web`), `## QA Verdict` (`qa`), `## Adversarial Audit Report` (`adversary`). See each agent's `.md` for the exact template. Both `qa` and `adversary` close with `### Result: <PASS | FINDINGS>` — same key, so you parse one thing, not two. Pass these **verbatim** to the next agent — do not summarize, paraphrase, or drop sections. If an agent's output doesn't follow its required structure, that's itself a finding — ask it to reformat before passing it on, rather than trying to fix/interpret a malformed hand-off yourself.

## Step 0 — Check run history
Lee la seccion **"Mapa de conocimiento"** al inicio de `docs/runs/INDEX.md` — esa seccion, no el libro mayor completo que va debajo *(added 2026-09-07: la tabla son 47 filas y ~5.6K tokens, con columnas de duracion y tokens que no ayudan a recuperar nada; se leia entera en cada corrida)*. Busca el tema de la tarea, abre **solo** los run docs que el mapa liste para ese tema, y baja al libro mayor unicamente si necesitas el detalle de una corrida concreta.

Si una corrida previa ya resolvio una pregunta de esquema, un patron o una decision de diseño similar, pasasela al architect y al planner en vez de dejar que la re-deriven. No te saltes esto ni en una tarea "simple" — el punto entero es no re-investigar terreno conocido. Si el mapa no tiene ningun tema que encaje, dilo explicitamente: es terreno nuevo, no una excusa para leer todo el libro mayor por si acaso.

**Business context check**: if the task touches customer contact data (phone/address typing), CRM case/interaction history, account balances, or anything else that sounds like a planned-but-not-yet-built feature, also check `contexto-negocio/` (shared vault root, not inside `api-core`) — especially the legacy ERD notes (real entity/field/FK detail, not a guess) and any vendor-proposal summary. These documents already answer questions like "does this catalog/table exist" that would otherwise require a fresh MCP schema dig — point the architect/planner at the specific file instead of letting them re-derive it.

**Un pendiente viejo se verifica antes de trabajarlo** *(added 2026-09-09)*: si vas a tomar una fila de `TASKS.md`, un hallazgo de un run doc o una afirmacion de `CLAUDE.md` cuya fecha es anterior a la ultima corrida de `INDEX.md`, **comproba primero que siga siendo cierta** — una consulta MCP, un `grep`, un `git log`. Cuesta segundos; trabajar sobre una nota vencida cuesta la tarea entera. El 2026-09-09 cinco notas distintas resultaron falsas en una sola sesion: el doc del sistema listaba tres skills inexistentes, un hallazgo de seguridad decia "sin trackear" sobre un archivo ya commiteado y pusheado, una migracion figuraba pendiente y estaba aplicada, `api-core/CLAUDE.md` daba por atrasada una base que estaba al dia, y dos items marcados como abiertos ya estaban resueltos. **Siempre la misma forma: el registro envejece mas rapido de lo que se relee.** Cuando la verificacion contradiga la nota, corregi la nota en la misma corrida — si la dejas, el siguiente vuelve a tropezar.

**Open business questions check** *(added 2026-08-18)*: also read `docs/PREGUNTAS-NEGOCIO.md` — a living list of business/product decisions someone still needs to make (not technical). If the task touches a question already listed there (e.g. role semantics, fields absent from the reference repos' schema, roles/permisos in `api-auth`), surface it to the architect/planner instead of re-deriving or re-asking the user from scratch. If the task's investigation surfaces a *new* business question that isn't yet on that list, add a row before finishing the run (Step 5) — don't let it evaporate into the conversation only.

**Retro cadence check**: also read the last dated entry in `docs/runs/RETRO-LOG.md` and count how many runs in `INDEX.md` are dated after it. If that count is 3 or more, mention it to the user as a one-line heads-up before continuing (e.g. "van 4 corridas desde la última revisión de retro — ¿la corro antes o después de esta?"). This is informational, like Step 2.6 — proceed with the requested task either way unless the user says otherwise; don't invoke `retro` yourself unasked.

**System-audit cadence check** *(added 2026-09-07)*: lee la última entrada fechada de `docs/runs/SYSTEM-AUDIT-LOG.md` y cuenta cuántas corridas de `INDEX.md` son posteriores. Si van **10 o más** (o si el archivo no existe todavía), avísalo en una línea antes de continuar: "van N corridas desde la última auditoría del sistema — ¿corro `system-auditor` antes o después de esta?". Informativo, como el chequeo de retro: sigue con la tarea pedida salvo que el usuario diga otra cosa, y **no invoques `system-auditor` por tu cuenta sin que te lo pidan**.

Cuando sí se corra, **invócalo con el parámetro `model: fable` del Agent tool** (sobreescribe el `model:` de su frontmatter, mismo mecanismo que el Step 2.7 usa para escalar el planner). Es una auditoría poco frecuente que decide la salud de todo lo demás — no es donde ahorrar. `system-auditor` no tiene `Write`: cierra su reporte con un bloque de entrada para el log, y **eres tú quien lo agrega** a `SYSTEM-AUDIT-LOG.md`. Lo mismo aplica al bloque `RETRO-LOG entry` de `retro`.

**Replay-harness check**: check `git log -1 --format=%ai -- .claude/agents/ .claude/commands/` **y también** `git status --short .claude/agents/ .claude/commands/` — las ediciones sin commitear nunca aparecen en `git log` (el 2026-09-07 había 9 archivos de agentes/comandos modificados o nuevos, todos sin commit, mientras `git log -1` seguía diciendo 2026-09-01). Compara contra la `fecha` de la corrida más reciente de `INDEX.md`. If any agent or command `.md` was modified after that date, or shows as modified/untracked (whether via a `retro`-approved fix or a direct edit), tell the user which agent(s) changed and ask: "¿corro primero una tarea dorada (`docs/runs/GOLDEN-TASKS.md`) para confirmar el cambio antes de esta tarea nueva, o seguimos directo?" Always ask — don't decide silently either way. If they say yes, run the relevant `/orquestar` pass for that golden task first (report its result plainly, pass or fail) before starting the requested task. If they say no or the task itself is urgent, proceed, but note in this run's Final report that the change is still unverified by replay.

## Step 0.5 — Create a run branch
**Con `--en`, la rama ya existe** — la creó `/worktree crear` — y este paso cambia entero *(added 2026-09-11)*:
- No hagas `git checkout -b`. Verifica que `git -C <RAIZ> branch --show-current` sea `feature/…` o `fix/…`, y que `git -C <RAIZ> log --oneline origin/develop..HEAD` muestre solo commits de esta tarjeta (vacío en un worktree recién creado).
- El chequeo de árbol sucio corre sobre `<RAIZ>`. La carpeta principal del repo puede estar sucia con otro trabajo y no importa, pero **guarda su `git -C <repo> status --porcelain`**: el Step 3 lo compara.
- El párrafo "Dependency on an unmerged prior branch" no aplica: en modo ola una tarjeta que necesita código de otra rama sin mergear estaba en la ola equivocada. Para y díselo al usuario en vez de mergear la otra rama.
- El HARD RULE de verificar que la rama tenga lo que la tarea asume sí aplica igual, contra `<RAIZ>`.

Sin `--en`, lo de abajo sin cambios:

Check `git status`. If the working tree is dirty (uncommitted changes unrelated to this run), stop and ask the user how to proceed instead of mixing their in-progress work into this run's branch. Otherwise, create and check out a new branch following the **org convention** (confirmed 2026-08-14 against the real commit/branch history of `api-contracts` and `api-people` on GitHub — do not invent a different scheme): `feature/<short-slug-en-minusculas-con-guiones>` for new functionality, `fix/<short-slug>` for bug fixes. No run-number prefix (`agent/NNNN-...` was an early convention for this skill and is deprecated — the run number lives in `docs/runs/NNNN-....md`, not in the branch name). Base branches in the org are `main`/`develop`/`qa`. All coder edits in this run happen on this branch. Do not push it — that stays a manual, explicit decision by the user.

**Dependency on an unmerged prior branch** *(added 2026-08-18, after 3 runs in one session needed this)*: if the requested task explicitly depends on code that only exists on a different branch not yet merged to `develop` (e.g. "add X to the endpoint built in the JWT-auth branch"), don't silently base the new branch on `develop` and hit missing code later, and don't silently base it on the dependency branch either — ask the user which they want: (a) merge the dependency branch to `develop` first, then branch from `develop` (cleaner history, recommended default), or (b) base this run's branch directly on the dependency branch, accepting that both stay unmerged together for now. If mid-run you discover a dependency that wasn't obvious upfront (e.g. a doc/ADR the architect needed turns out to live only on another local branch), don't treat it as a dead end — merge or cherry-pick the needed commit(s) into the current branch and note it in the run doc, same as you'd note any other mid-run correction.

**HARD RULE — verify the new branch actually has what the task assumes exists, before invoking the architect** *(added 2026-08-19, after the architect ran a full investigation against a branch that silently lacked 5 entities/features the task description referenced as already built — `develop` was stale relative to 2 open, unmerged PRs from the same session)*: never trust conversation memory or the task prompt's own claims about "X already exists" — verify it against the actual branch you just checked out. Immediately after `git checkout -b`, if the task references any specific prior-run artifact by name (an entity, a service, a config value, "the pattern from run NNNN"), do a quick `grep`/`Read` for it on the new branch **before** handing the task to the architect. If it's missing and you know which branch/PR has it (recent run docs / `INDEX.md` / open PRs), merge or cherry-pick it in immediately — don't let the architect (or planner) spend a full pass investigating against a stale snapshot and produce a report full of false "this doesn't exist yet" gaps that then have to be re-run from scratch. This is expensive to catch late (a wasted architect pass) and cheap to catch early (one grep).

**Run this pipeline in parallel with a standalone agent when the two don't depend on each other's output** *(added 2026-08-19)*: when the task has an independent UI piece (`ux-researcher`, a mockup) alongside this backend pipeline, and both start from a design already agreed with the user (not from each other's code — neither needs to read the other's output to begin), launch the standalone agent in the background (`run_in_background: true`) and continue this pipeline in the foreground immediately, rather than waiting for one to finish before starting the other. This only applies when the process genuinely doesn't need to be sequential — if the backend's exact shape (entity/field names, endpoint contracts) isn't settled yet, settle that with the user first, then launch both; a mockup built against an unsettled backend shape will need rework once the real shape lands. Real example: a case-history feature (2026-08-19) launched its console mockup update and this backend pipeline at the same time, both grounded in the same design already confirmed with the user — cut wall-clock time roughly in half versus running them one after the other.

## Step 0.6 — Identificar la tarjeta del tablero ANTES de empezar
*(added 2026-09-08, pedido explícito del responsable del repo — "preguntar al inicio si hay tarjeta o crear una")*: la tarjeta se resuelve al **inicio** de la corrida, no al final junto con el PR. En una corrida anterior se llegó al Step 4.7 con el PR ya abierto y recién ahí se buscó la tarjeta, contra un export de doce días que mentía en columnas e IDs — trabajo tirado y una pregunta al usuario que podría haberse hecho al principio.

**Consultá el tablero vivo por API, nunca un export.** Un export tiene fecha y se desactualiza: columnas que ya no existen, ids con colisiones. Sirve para leer notas técnicas, no para apuntar una mutación.

Preguntá al usuario: **¿esta tarea corresponde a una tarjeta existente, o hay que crear una?**
- **Existe:** su título **no se toca**. Anotá su id y URL.
- **No existe:** proponé título siguiendo la convención del tablero — título del PR + `(repo)` — y esperá aprobación expresa antes de crearla.

Llevá la tarjeta elegida hasta el Step 4.7, en vez de redescubrirla al final.

**Todo acceso al tablero va por una herramienta determinista que simula antes de escribir** — `buscar` para ofrecer tarjetas candidatas, `crear` para la nueva. No escribas Python suelto contra la API. Si `buscar` devuelve muchas candidatas, pasá la salida por el shunt en vez de leer el resultado crudo (ver [`/shunt`](shunt.md)). *(En el original esto era una skill sobre la API de Trello con los ids del tablero del cliente; se retiró de la versión pública.)*

## Step 0.7 — Tiered eligibility check (Nivel 0 / Nivel 1 / pipeline completo)
*(added 2026-08-26, the repo owner's explicit design — "a veces cambio color, disposicion, nombre de texto... la mayoria pase por ahi"; refined the same day — "la cosa es gastar lo menos posible si son cambios super simples")*: not every task earns a full `architect` investigation, and not every trivial task even needs a subagent. Default to the lightest tier that's honestly justified; escalate only when the task actually needs it — don't default to full-weight out of caution when nothing here applies, and don't reach for Nivel 0 when you're not genuinely certain it qualifies.

**Nivel 0 — you edit it directly, no subagent at all.** Reserve this for changes with zero logic and zero schema/security surface: a hardcoded string/label/color literal, a log message wording, a config constant, a code comment, a typo fix — the kind of change where "what should this say" has one obvious answer and there's nothing to design, plan, or architecturally verify. You (the orchestrator) make the edit yourself with Edit/Write, same as you would in any non-`/orquestar` conversation. Still required, no exceptions: run `dotnet build` (and the relevant `dotnet test` project if the touched code has coverage) to confirm nothing broke, and still commit + record Step 5 in its lightest form (an `INDEX.md` row, no `NNNN-<slug>.md` — see below). This is not "skip the safety net", it's "the safety net that matters here is a green build, not an architect pass on a string literal".


**Nivel 0 también en web-app, y aplicado de verdad** *(added 2026-09-14, pedido explícito del responsable del repo para desarrollar más rápido)*: una etiqueta o texto, quitar o mover un botón, redirigir un botón a una vista que ya existe, una clase de estilo o un valor de tamaño son Nivel 0 — los hace el orquestador directo en 2-3 minutos, no un coder que tarda 10-15 porque arranca leyendo el código desde cero. Real: cambiar una etiqueta (Nivel 0, 2 min) frente a quitar un botón del rail y redirigir a otra vista (se mandó a un coder, ~10 min). Red de seguridad de Nivel 0 en web-app, en lugar de `dotnet build`: `npx tsc -b`, `npx eslint` sobre los archivos tocados y `npx vitest run` de los tests que los cubren (si un test existente referencia el texto o el botón, se actualiza en el mismo cambio). Deja de ser Nivel 0 si el cambio toca estado, navegación con parámetros, datos o una interfaz con api-core.

**Juntar pedidos antes de lanzar, en demos en vivo** *(added 2026-09-14, mismo pedido)*: cuando el responsable del repo va pidiendo ajustes seguidos mientras mira la pantalla, no lances un coder por cada mensaje ni le mandes cambios a uno que ya está trabajando (cada cambio a mitad de camino lo obliga a re-planear). Espera a que cierre la idea (un minuto, o hasta que diga "dale"/"ya"), agrupa los pedidos, sepáralos en Nivel 0 (los haces tú) y cambios con forma (un coder por cambio independiente, en paralelo — ver Step 3), y lánzalos de una vez. Excepción: un bug que bloquea la demo se lanza enseguida.
**Nivel 1 — fast-path** (skip Step 1 — architect — only, go straight to Step 2 with the task as-is): the change is cosmetic/mechanical but still has *some* real shape to get right — a status/color **mapping** (multiple cases, not one literal), a field rename that touches a DTO other code depends on, a one-line bug fix that doesn't touch schema/data-access/auth/concurrency, reordering/adding a field to an existing response. The defining trait: there's no real "which pattern should this follow" question — the pattern is already obviously the one right there in the file being touched — but it's still real enough to want planner+coder+QA's eyes on it, just not a dedicated architect investigation.

**Never Nivel 0 or Nivel 1 — always run the full sequence**: new endpoints, any schema/migration change, anything touching `ADR-0001`'s People/Contracts/<sistema-legacy> boundaries, auth/JWT/roles, concurrency-sensitive patterns (claim-a-row, single-active-X), or anything you're genuinely unsure how to classify. When in doubt, run the full pipeline (or at least Nivel 1) — these lighter tiers are for the obviously-safe majority, not a default you talk yourself into for something borderline. If you're not sure whether something is Nivel 0 or Nivel 1, pick Nivel 1 — the cost difference between them is small (one subagent call), the cost of a Nivel-0 change turning out to need real judgment is not.

**What Nivel 1 skips vs. keeps**: skips Step 1 (architect) only. Step 2 (planner) still runs — tell it explicitly this is fast-path so it keeps the plan proportionally short — and Step 3 (coder/coder-web) and Step 4 (QA, Nivel 1 testing tier) still run unchanged; Step 3.5 y los tres gates del Step 4.4 aplican igual que en el pipeline completo (una corrida Nivel 1 anterior sí corrió `adversary`); those are the safety net that protects the reason `/orquestar` exists in the first place — a 2026-08-15 incident where code was edited directly in chat left no ADR, no run-doc entry, and skipped the planner's schema verification and QA's checklist entirely. Bypassing QA/schema-verification is the actual thing that's not allowed here, not "always spend a full architect pass on a label rename". Step 2.5/2.6/2.7 naturally no-op for a Nivel 1 task (its Risk Flags will read "no") — don't force them. Step 4.6 (security review) is still the orchestrator's judgment call for Nivel 1: skip it explicitly (state why in the Final report) only when the change plainly has no PII/auth/data surface. Nivel 0 skips security-review outright — there's no code path a security review would meaningfully evaluate in a string literal.

**Step 5 stays mandatory at every tier, in its proportional form.** For Nivel 0 or Nivel 1, a full run doc is disproportionate — use the lightweight `INDEX.md`-only row this project already does for small fixes (e.g. "*(fix chico, sin doc de run propio — ver commit `<hash>`)*", see the existing small-fix rows in `INDEX.md`) instead of writing `NNNN-<slug>.md`. This is still Step 5, just its proportional form — the "run is not done until Step 5 is done" rule from Step 5 below still applies in full, at every tier including Nivel 0.

## Step 0.8 — REQ confirmado (Spec-Driven Development, solo pipeline completo)
*(added 2026-09-18, decisión explícita del responsable del repo)*: si la tarea califica para el **pipeline completo** (ni Nivel 0 ni Nivel 1, ver Step 0.7), verifica primero si tiene un REQ **confirmado** en `requisitos/` (índice en `requisitos/README.md`, carpeta local sin git). Si no lo tiene, para antes del Step 1 y dile al usuario: falta el REQ, ¿lo redactamos con la skill `/requisitos` antes de seguir, o esta tarea entra sin REQ por excepción? No lo asumas ni lo saltees en silencio.

Nivel 0 y Nivel 1 **no** requieren REQ — sería desproporcionado exigirle spec formal a un fix cosmético o de una línea.

**Si el REQ cambia de versión mientras el coder ya está trabajando sobre la versión vieja**: no relances el planner por tu cuenta. Para la corrida y repórtale al responsable del repo qué versión estaba implementando el coder y qué cambió — mismo criterio que ya aplica cuando el coder o QA encuentran que el plan mismo está mal (Step 2, "stop and report to the user instead of silently sending it back to the planner"). El responsable del repo decide si el coder sigue, se descarta lo hecho, o se re-plani­fica.

## Step 1 — Architect
Invoke the `architect` subagent (`.claude/agents/architect.md`) with the task. Its job: find the closest equivalent pattern in `api-contracts` or `api-people` and recommend how to build this consistently with them.

Wait for its output before continuing. If it proposes concrete design decisions beyond "which pattern to follow" (e.g. specific tech choices like adding a new package), carry those forward as *proposed* decisions for the planner to confirm or reject — don't treat them as final yet.

## Step 2 — Planner
Invoke the `planner` subagent (`.claude/agents/planner.md`) with the task plus the architect's full output. Explicitly instruct it to:
- Confirm or correct every decision the architect proposed.
- Verify claims against the real DB schema via the `contracts-db`/`people-db` MCP connectors rather than trusting the reference-repo pattern blindly (this caught real bugs in the first pilot — schema name mismatches, nullable columns, unnecessary joins — keep doing it).
- Cross-check against `docs/adr/` — flag if the plan would touch anything ADR-0001 governs (writes to <sistema-legacy>, People schema changes, phone typing) so Step 2.5 can catch it.
- Produce a numbered task list and a QA test matrix (functional cases, response-contract checks, edge cases).

This is the planner's only pass — it does not get invoked again later in this run. If the coder or QA later find the plan itself is wrong (not just misimplemented), stop and report to the user instead of silently sending it back to the planner.

## Step 2.5 — Human checkpoint for risky plans
Check the planner's `### Risk Flags (for orchestrator Step 2.5)` section directly — don't re-derive this yourself, the planner already evaluated it. If any flag is "yes": **stop and present the plan to the user for explicit approval before continuing to Step 3.** Summarize what's risky and why in 3-5 lines — don't just dump the whole plan and ask "ok?". Wait for an explicit yes. If none apply (typical case: read-only endpoint, no schema change), continue straight to Step 3 without pausing.

## Step 2.6 — Cost heads-up for large tasks
Before Step 3, check the planner's `### Task List` size and compare against `docs/runs/INDEX.md`'s Tokens column for any prior run of similar shape (a full endpoint = light, a structural refactor or multi-file change = heavy — a layered-project split in an early run cost ~167K tokens as a reference point). If this task looks similarly large, mention the estimated cost as a one-line heads-up to the user before continuing (e.g. "Esto luce parecido a la separación por capas, que costó ~167K tokens — sigo salvo que digas otra cosa"). This is informational, not a blocking gate like Step 2.5 — proceed after saying it unless the user actually objects. Skip this step silently for small/typical tasks (a single new endpoint, a small fix).

## Step 2.7 — Opus escalation for the planner (opt-in, only on already-flagged risk)
The `planner` gets exactly one pass — no revision — so it's the single point of failure most worth paying for. But only offer Opus when Step 2.5 or Step 2.6 already flagged real risk/size (any Risk Flag "yes", or a task judged similarly large to that split) — never as a default, and never for a typical single-endpoint task. When that condition is met, ask the user once, before re-running the planner: "Esto califica como plan de alto riesgo/tamaño — ¿quiero que el planner corra en Opus en vez de Sonnet? Cuesta más tokens, pero es el único paso sin segunda pasada." If the user already produced a plan on Sonnet before this check ran, don't discard it — only apply this on the planner's first invocation for the run. Never escalate `architect`, `coder`, or `qa` this way — their mistakes are already caught by a downstream role, so the redundancy substitutes for a stronger model there.

## Step 3 — Coder (elige cuál según qué toca el plan)

**Tres palancas más de tiempo, medidas en la corrida del 2026-09-15** *(el responsable del repo: "y se puede hacer más rápido?")*. Esa corrida costó ~60 min de reloj (79 min de cómputo de agentes) para un rename en dos repos con cambio de interfaz, edición de migración y baja de 4 columnas. De dónde se sacan los minutos, en orden de impacto:

1. **Decidir TODO antes de lanzar la primera ola.** Es la grande, y el cuello de botella es el orquestador, no los agentes. En esa corrida se lanzó el rename y la decisión de qué borrar llegó después; como la limpieza tocaba los mismos archivos, tuvo que esperar a que el rename terminara — 14 min en serie por repo, ~28 en total. El orden correcto es **investigar todo → decidir todo con el usuario → lanzar una sola ola**, no investigar → lanzar → decidir → lanzar otra vez. Si hay un inventario, un Step 2.5 o cualquier aprobación pendiente que vaya a tocar los mismos archivos que la parte mecánica, se resuelve **antes**.
2. **Un solo coder por repo cuando dos trabajos comparten archivos.** Rename y limpieza tocaban `CrearCasoCommandHandler`, `CasoController` y medio `src/` de web-app. Dos coders en serie sobre el mismo conjunto de archivos cuestan más que uno solo haciendo ambas cosas (~10 min por repo). Paralelizar es por defecto, pero **compartir archivo es la excepción**: ahí se junta, no se encola.
3. **Partir un rename grande por carpeta.** 46 archivos en un coder fueron 21 min; tres coders por área (`features/cases`, `features/agent-console`, `features/messages` + `infrastructure`) lo bajan a ~8-10. Requisito para que no se rompa: escribir la **tabla de nombres viejo → nuevo antes de lanzar** y dársela idéntica a los tres, porque los imports cruzan carpetas.

**Lo que NO se recorta para ganar tiempo**, y en esa corrida se demostró por qué: la verificación contra la base con datos reales (probó que el bug de la migración era real — 14 filas ya referenciaban esas tipificaciones), el contraste del diff contra `git status`, y QA + `adversary` sobre cualquier cosa que borre columnas. En esa misma corrida **el `architect` y el `planner` alucinaron, por separado, un panel de UI que no existe** y sobre esa base recomendaron conservar dos columnas; sin la verificación propia del orquestador (`grep` devolvió 0 ocurrencias) se habría decidido sobre un reporte falso. El reporte de un agente no es evidencia.

**Adelantar los coders sin esperar al planner cuando el alcance ya está especificado** *(added 2026-09-15, pedido explícito del responsable del repo: "no puedes demorar menos tipo haciendo mas cosas a la vez?" y, al ver el resultado, "siempre aplicar asi las corridas en orquestar para evitar demorar tanto")*: el Step 2 es un cuello de botella innecesario para la parte **mecánica** de una corrida — un rename, una ruta que cambia, un fix ya descrito con `archivo:línea` por el architect o por una revisión de Codex. En esos casos no encadenes planner → coder: lanza los coders de lo mecánico **de una vez** y deja que el planner corra **en paralelo**, ocupándose solo de lo que necesita juicio y aprobación humana (qué código muerto se elimina, cómo se edita una migración, cualquier cosa que dispare el Step 2.5).

Receta, en orden:
1. **Fijá la interfaz antes de lanzar nada.** Si el cambio cruza repos, la firma exacta (ruta, nombres, tipos) se decide primero y se le escribe a cada coder como "INTERFAZ FIJA, no la negocies". Sin eso, paralelizar produce dos mitades que no encajan.
2. **Repartí por archivo, con dueño exclusivo.** Cada prompt lista explícitamente **qué archivos son suyos** y **cuáles tiene prohibido tocar**, nombrando al otro coder como dueño. Un archivo, un coder.
3. **Lanzá todos los coders de lo mecánico en el mismo mensaje**, en background.
4. **El planner corre a la par**, con el alcance recortado a lo que sí requiere diseño.

**Condición para que esto sea seguro, y no es negociable:** el architect ya verificó el patrón y la interfaz no se va a mover. Si el planner pudiera contradecir lo que el coder está escribiendo en ese momento, ese pedazo **no** se adelanta — va después del plan, como siempre. Tampoco se adelanta nada que toque esquema sin pasar antes por el Step 2.5.

Medición real de la corrida que originó esta regla (2026-09-15, rename de tres entidades en dos repos): estimada en ~40 min encadenando, cerró en ~20 — 4 coders (2 de api-core, 2 de web-app) arrancaron mientras el planner y el inventario de código muerto todavía corrían.

**Repartir el gasto entre cuentas, y no confundirlo con eficiencia** *(added 2026-09-15, pregunta del responsable del repo: "¿por qué le sale más barato, es porque tenemos tantos agentes?")*. No: **los tokens de Codex y de Antigravity salen de otras cuentas** (ChatGPT y Google), no de la de Claude. Delegarles trabajo mueve el gasto de un presupuesto a otro; no lo reduce en abstracto. Dos aclaraciones que evitan decidir mal:

- **La cantidad de agentes no multiplica el costo.** Cinco coders en paralelo gastan lo mismo que cinco en serie: lo que cambia es el tiempo de pared, no los tokens.
- **Sí hay una parte real de eficiencia**, pero es chica y tiene explicación: un `coder`/`coder-web` arranca leyendo `CLAUDE.md`, su propio `.md` de agente, el mapa de la consola y los archivos del proyecto. Codex arranca solo con la tarea escrita en el prompt. Por eso una tarea comparable le sale más barata en tokens absolutos (54K contra 63-338K de los coders propios en la corrida del 2026-09-15).

**Regla acordada — un coder de cada motor, en paralelo** *(el responsable del repo, 2026-09-16: "trata de balancear, quiero que se haga rápido pero bien; quizá 3 coders, uno de cada uno"; reemplaza el orden estricto Codex → Antigravity → Claude que se escribió una hora antes)*: cuando la corrida tiene 2 o 3 cambios independientes, se reparten **por naturaleza de la tarea**, uno por motor, todos a la vez:

| Motor | Qué le toca | Cómo |
|---|---|---|
| **Codex** | Mecánico con criterio verificable y un archivo con dueño: plomería de un campo, renombres, un fix ya descrito con `archivo:línea` | `codex exec --sandbox workspace-write`, condiciones en `/codex` |
| **Antigravity** | Visual y de estilo dentro de componentes: clases, tokens, jerarquía, pasadas de color | `agy --model gemini-3.1-pro-high --mode accept-edits --print="..."`; el prompt nombra los archivos prohibidos igual que a un coder propio |
| **code-write** (Antigravity, motor "shunt") | Mecánico y acotado a **un solo archivo nuevo o reescrito completo** a partir de spec + archivo de referencia (ej. un test que sigue el patrón de otro, un DTO calcado de uno existente) — no para archivos que ya existen con historia propia para editar en el medio | `bash ~/.claude/scripts/code-write.sh --spec "..." --reference <archivo> --target <archivo>` (ver [`/shunt`](shunt.md)); en modo plan, el script escribe a disco, no `agy` |
| **Claude** (`coder`/`coder-web`) | Lógica con estado que cruza varios archivos con dependencias, interfaces entre capas, y lo que exige las reglas de `CLAUDE.md` (aislamiento de sesión, agregación sobre colecciones completas) | Subagente nombrado, como siempre |

**`code-write` no reemplaza QA/adversary, los adelanta** *(added 2026-09-18, decisión explícita del responsable del repo al pedirlo)*: es un primer borrador barato para el caso angosto de "archivo nuevo calcado de uno existente", y ese borrador pasa por el mismo Step 4+ (QA, `adversary`, contraste contra `git status`) que cualquier otro coder — no es un atajo que salte el pipeline.

**Todo motor externo en segundo plano se lanza vigilado** *(2026-09-16, el responsable del repo: "eso no puede pasar")*, porque en una corrida anterior Codex quedó 15 minutos colgado esperando entrada por consola sin que nadie lo notara, y a la pregunta "¿cuánto falta?" se respondió con una estimación en vez de mirar: (1) **entrada cerrada** con `< /dev/null`; (2) **tope de tiempo** con `timeout` (p. ej. `timeout 20m codex exec ...`), así un cuelgue termina solo; (3) **a los 60 segundos, leer el log**: si dice `Reading additional input from stdin`, no crece o no aparece el archivo de salida, cortarlo y relanzar enseguida; (4) **nunca responder "cuánto falta" sin mirar primero** el log o el estado real del proceso. Si un motor no tiene crédito, su parte pasa al siguiente que pueda hacerla; si ninguno externo puede, a Claude, **y se dice en el reporte final**. Los diffs de Codex y Antigravity se contrastan contra `git status` y se verifican igual que los de un coder propio. **Lo que no se recorta por velocidad:** QA y la verificación del diff siguen en toda corrida. Motivo del cambio: el presupuesto de Claude es el escaso (en esa corrida dos `coder-web` gastaron ~329K y ~218K tokens), pero mandar todo a un solo motor externo pierde calidad y, en serie, velocidad.

**El tablero y los PRs se quedan en el orquestador para el contenido — no para la lectura mecánica** *(actualizado 2026-09-18, ver [`/shunt`](shunt.md))*: qué va en el título, la descripción, los miembros de una tarjeta o el cuerpo de un PR lo sigue decidiendo Claude — para eso ya tiene el contexto de la corrida en la conversación, y transferírselo a otro motor costaría más que escribirlo él mismo (esto no cambió). Lo que sí se delega es la parte que **no** necesita ese contexto: leer salidas grandes de solo lectura — búsquedas y listados del tablero, `pr diff`/hilos de comentarios/checks de GitHub — va por `gh-read-shunt.sh` (y el shunt equivalente del tablero) en vez de entrar cruda al contexto de Claude. Las escrituras ya decididas por Claude (simulación y después `--aplicar`) siguen corriendo directo: su salida ya es corta por diseño, pasarla por otro motor no ahorraría nada. `gh pr create`/`merge`/`close` nunca se delegan — es la única escritura de este grupo sin el gate mecánico de `--aplicar` que tiene el tablero, y correrla fuera del Bash tool de Claude se saltaría `git-guard.sh`.

**Modos de presupuesto** *(el responsable del repo, 2026-09-16: "dale prioridad a codex y antigravity, pq el claude se me está acabando"; "codex siempre debe quedar en cero")*. La corrida se lanza con `--modo <ahorro|normal|calidad>`; **sin flag, `ahorro`**, hasta que el responsable del repo diga otra cosa. **Codex se gasta primero y hasta agotarlo**: crédito de Codex que sobra al final del período es presupuesto perdido. Cuando Codex responde límite de uso, su parte pasa a Antigravity, y solo después a Claude.

| Paso | `ahorro` (por defecto) | `normal` | `calidad` |
|---|---|---|---|
| Planner | `codex exec -s read-only` con `.claude/agents/planner.md` como instrucciones. Si hace falta leer bases por MCP (Codex no las tiene), `planner` de Claude con `model: sonnet` | `planner` de Claude | `planner` de Claude (Fable si es Nivel 3) |
| Coders | Codex todo lo que no sea visual, **incluso cambios que cruzan archivos** (una tarea `.md` por coder, archivos con dueño exclusivo); Antigravity lo visual | Un motor por cambio (tabla de arriba) | `coder`/`coder-web` de Claude + motores externos |
| QA | Comandos por shell (`tsc`/`eslint`/`vitest` o `dotnet build`/`test`) + `codex exec review --uncommitted` + Antigravity si toca UI | `qa` de Claude + Codex | `qa` de Claude + Codex + Antigravity |
| Adversary | `codex exec -s read-only` con `.claude/agents/adversary.md` como instrucciones, solo si la corrida toca estado, concurrencia, datos o seguridad | `adversary` de Claude | `adversary` de Claude |
| Claude hace | Orquestar, verificar cada diff y hallazgo contra el código, medir en Chrome (solo Claude maneja el navegador), hablar con el responsable del repo, run doc, commit | Lo anterior + los agentes marcados | Todo |

En `ahorro`, un subagente de Claude solo se lanza si ningún motor externo puede hacerlo o no tiene crédito, **con `model: sonnet`**, y el reporte final dice cuál y por qué. Lo que no baja en ningún modo: verificar el diff contra `git status`, la suite completa antes del commit y la vigilancia de los motores externos (párrafo anterior).

**Acotar el alcance de cada coder** *(added 2026-09-15, el responsable del repo: "si esta bien mejora eso")*. El coder más caro de esa sesión fueron **338K tokens**: el rename de 46 archivos de web-app en un solo agente leyendo todo desde cero, 21 minutos. Partirlo por carpeta (`features/cases`, `features/agent-console`, `features/messages` + `infrastructure`) cuesta parecido en total pero termina en un tercio del tiempo — y sobre todo, **un coder que se equivoca cuesta un tercio de retrabajo**. Requisito para que no se rompa: la tabla de nombres viejo→nuevo se escribe **antes** y se les da idéntica a los tres, porque los imports cruzan carpetas.

**No repetir trabajo: las cuatro pérdidas del 2026-09-15 y cómo se evitan** *(added 2026-09-15, el responsable del repo: "ya dale tambien eso no debe pasar de nuevo")*. Ninguna fue un misterio; las cuatro tienen causa identificable:

| Lo que se perdió | Causa | Cómo se evita |
|---|---|---|
| Dos agentes alucinaron un panel de UI que no existe, y el orquestador tuvo que verificarlos | Citaron `archivo:línea` sin haber corrido la búsqueda | Ya corregido en `architect.md` y `planner.md` por la retro de ese día |
| `security-review` revisó los `.md` del repo de agentes en vez del código de la corrida | El skill calcula su diff contra `origin/HEAD` desde la carpeta principal y **no se puede acotar** | **En modo `--en`, hacer el gate de seguridad a mano** sobre el diff real del worktree: autorización de los endpoints tocados, SQL literal vs interpolado, y qué datos destruye una migración. Decirlo así en el reporte |
| Dos intentos de arrancar los servidores de desarrollo | Se usó `nohup ... &` **dentro** de un comando en background: el proceso padre terminó y se llevó al hijo | Lanzar el proceso con el background del harness, **sin `&` ni `nohup`**, y esperar con un `Monitor` que chequee el puerto |
| Un intento de `agy` con la sintaxis mal | El prompt suelto hace que el CLI tome `--model` como prompt, ignore el resto y **salga con código 0** | Ya documentado en [`/antigravity`](antigravity.md): el prompt va pegado al flag |

La forma común de las cuatro: **algo falló en silencio y pareció haber funcionado**. Cuando un comando devuelve éxito, conviene verificar que hizo lo que se esperaba, no que no dio error.

**Codex también puede ser coder, no solo revisor** *(added 2026-09-15, primera vez que escribe código en este proyecto)*: `codex exec --sandbox workspace-write` le deja escribir en el worktree. Funciona, con condiciones estrictas:
- **Un archivo con dueño exclusivo**, nunca un cambio que cruce varios con dependencias entre sí.
- **Criterio objetivo y verificable** ("que no quede ninguna clase `dark:` en este archivo"), no una decisión de diseño.
- El prompt le nombra **explícitamente qué archivos tiene prohibido tocar** y quién es el dueño de cada uno, igual que a un coder propio.
- Su diff se contrasta contra `git status` igual que el de cualquier coder. En la primera prueba respetó su alcance y lo declaró con honestidad ("Modifiqué únicamente AccountTabs.tsx y su test"), pero eso se verifica, no se asume.

Real: el 2026-09-15 reescribió una función de tema de un componente de pestañas para sacarle la variante `dark:` de Tailwind, dejó un test que rechaza que alguien la vuelva a meter, y corrió sus tres comandos de verificación. *(La skill `/codex` todavía dice que Codex "no escribe código, el arreglo lo hace el coder de /orquestar" — esa línea quedó vieja y hay que actualizarla.)*

**Varios coders en paralelo, por defecto** *(added 2026-09-14, pedido explícito del responsable del repo: "siempre que se pueda debes usar varios coders así en paralelo, para ahorrar tiempo")*: antes de lanzar un solo coder, parte el plan en cambios independientes. Si hay 2 o más que tocan funciones o archivos distintos, lanza **un coder por cambio a la vez** (`run_in_background: true`, todos en el mismo mensaje), no uno detrás del otro. Cada prompt declara **qué funciones/archivos son suyos y cuáles no tocar**; si dos cambios comparten archivo, se asignan regiones distintas y se exige releer el archivo justo antes de cada Edit. Un solo coder solo cuando los cambios dependen entre sí (uno necesita el código del otro) o tocan la misma función. Si llega un cambio nuevo mientras un coder trabaja y es independiente, va a un coder nuevo en paralelo, no a la cola del que ya corre. Paralelizar no recorta tests: cada coder escribe los tests de su cambio.
Invoca el subagente **nombrado** que corresponda — por ese nombre exacto, nunca el agente genérico — con el plan final del planner como única fuente de verdad. Implementa, verifica que compile, y reporta exactamente qué cambió.

- El plan toca **backend .NET** (`api-core/`, `api-contracts/`, `api-people/`, `api-auth/`, `api-delivery/`) → `coder` (`.claude/agents/coder.md`).
- El plan toca **`web-app/`** (React/TypeScript) → `coder-web` (`.claude/agents/coder-web.md`).
- El plan toca **ambos** → invoca los dos, cada uno con la parte del plan que le corresponde, y consolida los dos `## Coder Report` en uno antes de pasárselo a QA. No le des el plan entero a uno solo.

*(added 2026-09-07: dos corridas anteriores editaron `web-app` con el `coder` de backend, cuyo prompt es 100% C#/EF Core — las reglas de frontend de `CLAUDE.md` (agregación sobre colecciones completas, aislamiento de sesión, decodificación de JWT) no tenían ningún agente que las ejecutara.)*

**No repitas la verificación del coder** *(added 2026-09-11, pedido del responsable del repo tras medir la sesión de la demo)*: el `## Coder Report` ya trae `build`/`tsc`, tests y lint, y QA los vuelve a correr por su cuenta en el Step 4 — eso ya son dos pasadas. **Tú no corras una tercera.** Léelas del reporte y sigue. Solo las ejecutas tú mismo en tres casos: el reporte no las incluye, se contradice con lo que ves en el diff, o el coder declara rojo y necesitas confirmar el alcance antes de decidir. Si las corres, di por qué en el reporte final.

**Con `--en`: verifica la carpeta antes de QA** *(added 2026-09-11)*. Esto sí es tuyo y es barato. Apenas vuelve el `## Coder Report`, confirma que los cambios cayeron donde debían: `git -C <RAIZ> status --porcelain` lista los archivos del reporte, y `git -C <repo> status --porcelain` de la carpeta principal sigue igual a lo que guardaste en el Step 0.5. Si el coder editó la carpeta principal, **para**: no muevas archivos a mano ni sigas a QA; muéstrale al usuario qué quedó en qué carpeta. Es un fallo silencioso — el build del worktree pasa igual, porque no ve el cambio.

## Step 3.5 — Architect en modo revisión (condicional, no en cada corrida)
*(added 2026-09-07; retiming 2026-09-18 — ver Step 4)*: si el `## Coder Report` muestra que la corrida **cambió estructura** — proyecto/capa/carpeta nueva, `DbContext` nuevo, middleware nuevo, namespace movido, o un patrón que no existía en el repo — invoca de nuevo a `architect`, esta vez con el `## Coder Report` como entrada, y trata sus `### Deviations (if reviewing existing code)` como hallazgos de QA. **Presupuesto:** cada ida al coder por una desviación **consume uno de los 2 ciclos del retry loop del Step 4** — no tiene presupuesto propio, porque si no una misma corrida puede rebotar cuatro veces entre agentes distintos. **Quién re-verifica:** QA en el Step 4, no una tercera pasada del architect — ya no corre "justo después", porque el Step 4 se pospuso hasta el gate de pre-PR (ver abajo), pero sigue siendo la instancia que valida esta desviación cuando ese gate finalmente corre; una pasada extra del architect ahora costaría lo mismo sin agregar nada. Si aplica, lanza este paso apenas vuelve el `## Coder Report`, antes del checkpoint commit del Step 3.6 — sus hallazgos van al coder en el mismo paquete que cualquier corrección de esa pasada, sin esperar al gate del Step 4.

Si la corrida **no** movió estructura (lo normal: un endpoint más, un campo más, un fix), **sáltate este paso** y dilo en el reporte final. QA item 3 y los tests de arquitectura ya cubren esa deriva mucho más barato que una pasada completa del architect.

## Step 3.6 — Checkpoint commit (antes del gate de QA/Adversary)
*(added 2026-09-18, pedido explícito del usuario: "no tiene que ejecutarse a cada rato, solo antes de hacer PR, o subir cambios a un PR, o cuando yo lo diga expresamente" — reemplaza el disparador inmediato que el Step 4 tenía desde su creación, donde QA y `adversary` corrían apenas terminaba cada coder)*: apenas vuelven el coder y, si corrió, el architect en modo revisión del Step 3.5, comitea el diff tal cual — mismo estilo de mensaje que describe el Step 4.5 (conventional commits en español), pase o no pase nada todavía. Esto reemplaza al commit que antes ocurría recién después de los tres gates del Step 4.4: ahora se adelanta, para no dejar trabajo sin guardar mientras el gate de QA/`adversary` se pospone hasta el momento del Step 4 (ver abajo). Si la corrida tiene varios coders en paralelo, un checkpoint por coder en cuanto termina, no uno solo al final.

## Step 4 — QA (y `adversary` en paralelo) — gate de pre-PR

**Cuándo corre este gate** *(added 2026-09-18, pedido explícito del usuario: "no tiene que ejecutarse a cada rato, solo antes de hacer PR, o subir cambios a un PR, o cuando yo lo diga expresamente")*: este Step (y sus gates 4.4/4.4a/4.4b/4.6) **ya no corre automáticamente después de cada coder**. Corre una sola vez, sobre el diff acumulado desde el último gate (no solo el último coder, para no dejar cambios sin auditar), en cualquiera de estos momentos:
1. Justo antes de ofrecer push/PR (Step 4.7).
2. Justo antes de subir commits nuevos a un PR que ya está abierto, si la corrida sigue agregando cambios después de un PR previo.
3. Cuando el usuario lo pida expresamente, en cualquier punto de la corrida.

Hasta que llegue uno de esos momentos, sigue con Step 3/3.5/3.6 (coder, revisión estructural si aplica, checkpoint commit) para el resto de la corrida sin invocar QA ni `adversary`.

**Revisión en paralelo, dentro de un mismo gate** *(added 2026-09-14, mismo pedido: "en revisión y eso también"; retimed 2026-09-18)*: si varios coders terminaron desde el último gate, cuando llega el momento de correrlo lanza un QA por cambio a la vez, sin esperar entre ellos. Mientras corren los QA, el orquestador verifica el diff contra `git status` y lanza la suite completa en segundo plano; todo eso a la vez, y los hallazgos vuelven a cada coder en un solo paquete.

**Codex como segundo revisor, en toda corrida salvo Nivel 0** *(added 2026-09-14; ampliado 2026-09-16, pedido: "trata de usar en lo posible a codex y antigravity" — antes era solo en el pipeline completo)*: en Nivel 1 y en el pipeline completo, cuando llega el momento de este gate, lanza la skill [`/codex`](codex.md) con `--uncommitted` sobre el repo o worktree **en paralelo con el QA**. Sus hallazgos se verifican contra el código y los reales vuelven al coder en el mismo paquete que los del QA. Si Codex no tiene crédito o no está logueado, se omite y se dice en el reporte final; nunca bloquea la corrida. Motivo: en dos PRs grandes Codex encontró 18 hallazgos en tres rondas después del push.
**Antigravity como revisor de lo visual** *(added 2026-09-15, primera prueba real; el responsable del repo: "podemos usar antigravity y codex siempre que podamos")*: El responsable del repo tiene el CLI de Antigravity instalado como `agy`, con modo sin interfaz. Es el **único revisor que mira cómo se ve el resultado** — QA, `adversary` y Codex miran corrección. Úsalo **cuando el diff toca UI**: estilos, layout, componentes, tokens de tema.

```
agy --model gemini-3.1-pro-high --effort high --mode plan --print="<prompt + diff>"
```

Tres cosas que hay que saber para usarlo bien:
- **El prompt va pegado al flag** (`--print="..."`), y `--model` va antes. Si se pasa suelto, el CLI toma `--model` como el prompt y descarta el resto sin fallar.
- `--mode plan` es el modo revisor: razona sin tocar archivos. `--json-schema` permite forzar salida estructurada (severidad, archivo, línea) en vez de prosa; vale la pena cuando el reporte se va a procesar.
- **Pásale el diff dentro del prompt**, no como archivo en el worktree: un archivo suelto ahí ensucia el `git status` de la corrida.

**Su tasa de acierto en la primera prueba fue 1 de 4** — pero ese 1 fue un badge que la limpieza de estilos había dejado sin forma, y **no lo vio nadie más**. Los otros tres: dos falsos positivos (afirmó que unos tokens no existían, y existen con 4 definiciones; afirmó que un contraste fallaba, y mide 6,25:1) y uno que fue culpa del orquestador (se le pasó un diff sin los tests y reportó "ausencia total de tests"). **Cada hallazgo suyo se verifica antes de mandarlo al coder.** Es una sola muestra: si en las próximas corridas la tasa no mejora, vale replantear si compensa.

Invoke the **named** `qa` subagent (`.claude/agents/qa.md`) with the coder's change summary and the planner's test matrix. It returns PASS or a list of findings.

**Lanza `adversary` a la vez que QA, no después** *(added 2026-09-11, pedido del responsable del repo tras medir la sesión de la demo: `adversary` esperando el PASS de QA agregaba 3-5 min de pared por corrida, y las dos leen exactamente el mismo diff)*. Los dos subagentes salen en el mismo mensaje, en background. Cuando vuelvan los dos, **consolida sus hallazgos en un solo paquete** y haz **un solo viaje** al coder, igual que ya hace el Step 4.4 con sus gates. Los topes siguen separados y no se mezclan: 2 ciclos para QA, 1 ronda para `adversary`.

Dos excepciones en las que conviene esperar el PASS de QA antes de gastar `adversary`: cuando el `## Coder Report` declara el build o los tests en rojo (el diff todavía no es auditable), o cuando ya estás en el segundo ciclo de QA por hallazgos funcionales.

## Retry loop (bounded)
- If QA returns findings: send them back to the **coder** (not the planner) for a fix. Re-invoke QA once more.
- Maximum **2** QA→coder→QA cycles total. If QA still has unresolved findings after that, STOP. Do not loop a third time. Report to the user: what QA found, what the coder tried, and that this needs human input.
- If QA returns PASS (at any cycle), stop immediately and report success — do not keep iterating for polish.

## Step 4.4 — Los tres gates corren en paralelo, y vuelven al coder en UN solo viaje
*(added 2026-09-07; ampliado 2026-09-11)*: quedan tres verificaciones — 4.4a (tests de integración), 4.4b (`adversary`) y 4.6 (`security-review`). **Las tres leen el mismo diff y ninguna depende del resultado de las otras.** Corrían en serie: eso es tiempo de pared regalado y, peor, hasta tres rondas separadas de ida y vuelta al coder por el mismo cambio.

Lánzalas solapadas, y desde 2026-09-11 **`adversary` arranca junto con QA** (ver Step 4), no después de su PASS:
1. Si 4.4a aplica (es un juicio tuyo, ver abajo), arranca `dotnet test api-core.IntegrationTests` **en background** — no te quedes esperándolo.
2. `adversary` ya está corriendo desde el Step 4; si por una de las dos excepciones de ese paso no lo lanzaste ahí, invócalo ahora.
3. Mientras tanto, corre tú el skill `security-review`.

Luego **consolida los tres resultados en un solo paquete de hallazgos** antes de mandar nada al coder. Un viaje, no tres. Los presupuestos de reintento no se mezclan: los hallazgos de `adversary` gastan el tope de 1 ronda del Step 4.4b, un fallo de integración gasta el de QA, y `security-review` no tiene loop automático (Step 4.6). Si el paquete consolidado mezcla varias fuentes, dilo explícitamente al reportarlo y trata cada tope por separado.

Si los tres salen limpios, dilo así de claro en el reporte final — "sin hallazgos" en los tres gates es información, no silencio.

## Step 4.4a — Integration tests (Nivel 2, orchestrator-run, once)
*(added 2026-08-26 — tiered testing strategy, the repo owner's explicit design)*: `qa.md` only runs unit + architecture tests (Nivel 1) during its retry cycles now — fast, no external dependencies, safe to re-run up to the 2-cycle limit. Integration tests (`api-core.IntegrationTests`, Testcontainers/Docker/SQL Server) are comparatively slow and don't need to run on every retry; running them once here, after QA has already returned PASS, gets the same coverage for a fraction of the cost.

**This is judgment, not a default-on step** — run it only when *you* (the orchestrator) decide it's actually warranted, based on what the run touched: schema/migrations, EF Core query changes, concurrency/locking patterns, anything reading or writing real data access — the kind of thing Nivel 1's mocks can't exercise. For a run that's purely additive/isolated with no data-access surface (a doc change, a config value, a pure-UI-adjacent change with no repository/DbContext touch), skip it — don't run it reflexively just because it's available. Either way, say explicitly in the Final report whether you ran it and why (or why not) — don't let the decision go unstated.

If you do run it (`dotnet test api-core.IntegrationTests`, yourself, not QA) and it fails, treat it like a QA finding: send it back to the coder, and this **does** count against the same 2-cycle retry budget as the QA loop above (it's still verifying the same change, just a slower layer of the same gate) — don't re-invoke QA's full Nivel 1 suite for an integration-only fix unless the fix could plausibly affect the Nivel 1 result too.

## Step 4.4b — Adversarial Code Audit (Nivel 3 — "Codex con esteroides")
*(added 2026-08-26, the repo owner's design — "un agente que solo vea las observaciones, heurísticas nivel producción Microsoft, nivel Codex con esteroides y análisis de mutabilidad")*:
Se lanza en paralelo con 4.4a y 4.6 (ver Step 4.4). Invoca el subagente **nombrado** `adversary` (`.claude/agents/adversary.md`).

Unlike QA (which verifies if the plan and tests passed), the `adversary` operates with an adversarial, destructive lens against the `git diff`:
1. Audits against the production heuristics and the 6 attack vectors — **las listas canónicas viven en `adversary.md` y no se repiten acá**: ya divergieron una vez (un archivo decía 8 sobre una lista de 10) y una enumeración parcial vuelve a invitar a lo mismo.
2. Conducts mental mutation testing to detect surviving mutants (critical bugs that existing unit tests wouldn't catch).

**Handling Verdict**:
- If `adversary` returns **PASS**: su parte del paquete consolidado del Step 4.4 queda vacía — espera a 4.4a y 4.6 antes del Step 4.5.
- If `adversary` returns **FINDINGS**:
  - P1 findings (compound failure corruption, SPA session leaks, concurrency races) must be resolved by the coder before opening the PR (Step 4.7); el checkpoint commit del Step 3.6 ya existe de todas formas, pase o no pase este gate — el fix, si lo hay, agrega el commit del Step 4.5 encima.
  - P2 findings and mutation test gaps: present them clearly to the user. If they represent real production risks, send to coder to fix.

**Bound — este loop también tiene tope** *(added 2026-09-07: no lo tenía, y una corrida registró 5 rondas de adversary→coder→adversary)*: **máximo 1** ronda adversary→coder→adversary, con su **propio** presupuesto, independiente de los 2 ciclos de QA (que a esta altura ya pueden estar agotados). Si después de esa ronda siguen quedando P1, **STOP** y escala al usuario igual que el retry loop de QA: qué encontró adversary, qué intentó el coder, y que esto necesita decisión humana. Todo fix originado aquí re-ejecuta `dotnet build` + los tests Nivel 1 antes de re-invocar a `adversary` — no la pasada completa de QA.

## Step 4.5 — Commit the result (correcciones del gate, si las hubo)
*(retimed 2026-09-18 — ver Step 3.6 y Step 4)*: el checkpoint del código del coder ya se comiteó en el Step 3.6, antes de que este gate corriera. Este paso es para el commit **adicional** que resulta de un hallazgo de QA/`adversary`/integración/seguridad: once the three Step 4.4 gates (4.4a, 4.4b and 4.6) have all returned and their consolidated findings package is resolved (coder applied fixes) or escalated — or the run is stopped/escalated after a retry limit — commit whatever state the branch is in — passing or not, so nothing is lost. Si el gate no encontró nada que corregir, no hay commit nuevo que hacer acá: el checkpoint del Step 3.6 ya es el estado final antes del push. **Commit message style: conventional commits, en español** *(decisión del responsable del repo del 2026-09-16, "siempre adoptar convención"; reemplaza la regla anterior de oración sin prefijo)*: `<tipo>(<alcance opcional>): <descripción>`, con la descripción en español, en imperativo y en minúscula — p. ej. `feat(casos): agregar endpoint de resumen`, `fix(consola): corregir filtro de la lista`, `docs: registrar corrida NNNN`. Tipos: `feat` (funcionalidad nueva), `fix` (corrección), `docs` (solo documentación), `refactor` (sin cambio de comportamiento), `test`, `style`, `perf`, `ci`, `build`, `chore`. Nunca en inglés ni verboso. Los commits de merge quedan como `Merge …`, igual que los que genera GitHub. A body with more detail (task summary, QA verdict) is fine below the subject line, same as this skill has been doing. Do not merge to the base branch.

**Keep low-signal-for-human-reviewer files out of the PR's visual noise via `.gitattributes`, without changing how/when anything is committed** *(added 2026-09-02, the repo owner's explicit request; expanded same day)*: everything still gets committed normally as part of Step 5, in the same PR as the code — a separate commit does **not** hide anything from GitHub's "Files changed" view (that view aggregates the full base→head diff regardless of commit boundaries, confirmed 2026-09-02 after an initial wrong assumption that a separate commit would achieve this). The actual mechanism: each repo's `.gitattributes` marks these paths as `linguist-generated=true`, so GitHub collapses them by default in the PR diff (still fully present, expandable with one click, zero effect on git history or on any tooling reading them normally — `architect`/`planner`/`retro` still read `docs/runs/` fine). Current set for `api-core` (patrones de `api-core/.gitattributes`, relativos a la raíz de ese repo — por eso sin prefijo `api-core/`; verify/replicate per-repo, don't assume every repo has the same file needs `.gitignore`-style):
- ~~`docs/runs/*.md`~~ — *(quitado 2026-09-16: los run docs se mudaron al vault y ya no viajan en los PR de api-core)*.
- `Persistence/Migrations/*.Designer.cs` — 100% auto-generated by `dotnet ef migrations add`, nobody hand-edits these.
- `api-core.Tests/**/*.cs`, `api-core.IntegrationTests/**/*.cs`, `api-core.ArchitectureTests/**/*.cs` — the repo owner's explicit call: the human reviewer doesn't review test diffs in practice anyway, and test quality is already gated by `qa`/`adversary` before the PR exists, so collapsing them doesn't remove review that was actually happening. This is a repo/team-specific judgment call, not a universal "hide tests from review" default — don't silently replicate it into a different repo's `.gitattributes` without asking.
- ~~`docs/adr/*.md`~~ — *(quitado 2026-09-16: los ADRs se mudaron al vault y ya no viajan en los PR de api-core)*.
- ~~`docs/architecture/**`~~ — *(quitado 2026-09-16 junto con los diagramas PlantUML)*.

Initial reasoning for excluding ADRs (superseded by the repo owner's explicit override above, kept here as the tradeoff record): ADRs are short, standard practice, and a reviewer would normally be expected to read them without prompting. If a future repo/session is deciding this fresh, that's still the right *default* to propose — just don't assume it's final without asking, the way it wasn't here.

## Step 4.6 — Security review (complements QA, doesn't replace it)
Se lanza en paralelo con 4.4a y 4.4b (ver Step 4.4). Corre el skill `security-review` contra el diff de esta corrida antes de ofrecer commit/push. **Carga además `.agents/skills/security/SKILL.md`** y pásale su checklist como criterio adicional — ese archivo tiene el catálogo CWE y las versiones mínimas de NuGet/npm, y hasta 2026-09-07 no lo referenciaba nadie. Si el `## Coder Report` lista un `.csproj` o un `package.json` cambiado, verifica explícitamente su sección de versiones: hoy nadie en el pipeline revisa la versión de un paquete al agregarlo. This is a separate, dedicated pass — QA's checklist item 5 only catches "obvious" issues in passing; api-core moves PII (national id, phone, address) and financial data, so it earns its own gate. Report what it found plainly. If it surfaces something real: do not auto-loop back to the coder the way QA findings do (that retry budget is QA's, not this pass's) — stop and tell the user what was found, let them decide whether to send it back to the coder or accept/defer it. A clean result is also worth stating explicitly ("sin hallazgos"), not silently skipped over.

**Scoping the diff correctly** *(added 2026-08-18)*: the skill computes its own diff against `origin/HEAD`, which you cannot override from the skill call itself. Two things go wrong in practice if you skip this: (1) if `git remote set-head origin -a` was never run in this environment, `origin/HEAD` can be unresolvable and the skill call fails outright — run it once (harmless, idempotent) before the first `security-review` call in a session if you hit that error. (2) When this run's branch stacks on another unmerged branch (common now per the Step 0.5 dependency guidance above), the diff against `origin/HEAD`/`develop` includes that other branch's already-reviewed changes too, not just this run's. When that happens, don't let the noise dilute the review — in your own summary of the skill's report, explicitly scope your read to the files this run actually touched (you know them from the coder's `## Coder Report`) and say so, same as noting "scope note" has worked in past runs.

## Step 4.7 — Offer push + PR (only if the GitHub MCP is connected)
**Antes de este paso, confirma que el gate del Step 4 ya corrió sobre todo el diff acumulado** *(added 2026-09-18)*: si hay commits del Step 3.6 posteriores a la última vez que corrió QA/`adversary` (por ejemplo, la corrida siguió agregando cambios después de un PR previo), corre el Step 4 sobre ese diff nuevo antes de ofrecer el push — nunca subas a un PR, nuevo o existente, código que el gate no haya visto todavía.
**Con `--en`, este paso no corre** *(added 2026-09-11)*: ni push ni PR, y no se ofrecen. Cierra diciendo la rama y el worktree, y que queda para `/worktree sincronizar`. La tarjeta del tablero no se mueve: pasa a revisión solo cuando el responsable del repo lo pide (ver abajo).

Check whether `github`-prefixed MCP tools are available (e.g. via `ToolSearch` or the tool listing). If they are NOT available, skip this step silently — just tell the user the branch name and that it's ready for their manual review/push, same as before this step existed. **Explicitly remind them the PR base must be `develop`, not `main`** — GitHub's own "Compare & pull request" banner defaults the base to the repo's default branch (`main`), and this has caused real PRs to land on `main` directly, skipping `develop`/`qa`, twice in one session (2026-08-17). If that happens anyway, `/verificar-ramas` diagnoses and fixes the resulting divergence.

**Consolidate stacked branches into one PR — default behavior, not a one-off** *(added 2026-08-19, after el responsable del repo explicitly asked for this as the standing convention going forward)*: when this run's branch was created from another unmerged run's branch (per the Step 0.5 dependency guidance), and that chain has more than one unmerged branch waiting on human review, do **not** open a separate PR per run. the repo owner's reviewer has to approve each PR into `develop`, and a stack of several small PRs is worse for them than one PR with the accumulated diff: reviewing N PRs in the right order is more overhead than reviewing one, and if the reviewer requests changes on an early PR in the stack, fixing it means merging that fix forward through every downstream branch one at a time — real, avoidable pain. Instead: only open (or keep open) a PR from the **tip** branch of the chain straight to `develop` — its diff already contains everything from the earlier unmerged branches in its history, so nothing is lost. If earlier runs in the same chain already have their own open PRs from prior sessions, close them with a comment pointing to the consolidated PR. This only applies to a chain of *dependent* branches (each one needs the code in the last) — independent branches that happen to run in parallel still get their own PRs, there's no dependency to consolidate.

If GitHub tools ARE available: ask the user explicitly (do not push or open a PR without a clear yes each time — a prior approval for one run does not carry over to the next) whether they want you to:
1. `git push` the run's branch to `origin`.
2. Open a PR against `develop` (the org's actual base branch, confirmed 2026-08-14 — not `main`), with a title in the same conventional-commits style as the commit subject (`<tipo>(<alcance>): <descripción en español>`) and a body summarizing: what was built, the architect's pattern, the QA verdict, any deferred findings — pulled from the Step 5 run doc content, not re-derived from scratch.

Only do what the user confirms — they may want the push but not the PR, or neither. Report the resulting branch/PR URL back to the user when done.

**Once the PR exists, do NOT move the board card to review** *(added 2026-09-10; changed 2026-09-19, pedido del responsable del repo: "la card solo pasa a revisión cuando lo digo yo", porque los PRs recién abiertos siguen recibiendo correcciones de bugs)*: only offer to attach the PR to the Step 0.6 card (simulate, show, then apply on approval). The card stays in its current column. Moving it to the review column plus members, labels and dates happens only when the repo owner explicitly asks for it — never offer it proactively at the end of a run.

**Never paste a token, secret, or credential into a commit message, PR body, or any tool call argument** — if a GitHub operation needs a credential beyond what the connected MCP already handles, stop and ask the user to act directly instead.

**Check for automated review bot comments on a still-open PR from a prior run** *(added 2026-08-20, after a Codex/GitHub automated reviewer caught 5 real issues — 2 of them race conditions — in a PR the architect→planner→coder→qa pipeline itself had already passed)*: before starting any new task, if the user mentions a reply/response/comment landed on an already-open PR from a previous run, don't assume it's necessarily human feedback — check with `gh pr view <N> --json comments,reviews` and `gh api repos/<owner>/<repo>/pulls/<N>/comments` (inline review comments, which the `reviews` field alone won't show). If the thread is long, run these through `gh-read-shunt.sh` instead of reading them raw *(added 2026-09-18, ver [`/shunt`](shunt.md))*. It may be an automated bot (Codex, CodeRabbit, etc.) that a service/bot account triggered. Treat its findings with the same rigor as a QA finding if they're concrete (file:line, a described failure mode) — don't dismiss them as noise just because they're bot-generated, and don't silently accept them as gospel either: verify each one against the real code, the way the planner already does for the architect's report, before building a fix plan around it.

## Step 5 — Record the run

**This step is mandatory, not optional — a completed run without it disappears from the project's history.** *(added 2026-08-18, after a real run — JWT Bearer auth added to every api-core endpoint, architect→planner→coder→QA 2 cycles→security review all passed — never got its run doc written because the conversation moved on to something else before Step 5 ran. The code shipped; the record of how/why/what-was-decided didn't. Do not let a later request pull focus away before this step completes — if the conversation is about to move to a new topic and Step 5 hasn't happened yet for the run that just finished, do it now, don't defer it "for later.")* **IMPORTANT** *(reinforced 2026-08-26, after the same omission recurred on a later run despite the warning above — it was phrased as advice, not a gate, and got skipped anyway when the conversation moved on): the run is NOT done until this step is done. Do not report the run as complete, do not tell the user it shipped successfully, and do not treat the branch/commit as the finish line, while Step 5 is still outstanding for a run that already reached QA PASS (or was stopped/escalated). If you notice mid-run that a PRIOR run's Step 5 was skipped, write that doc now, before starting anything else — same as this run's own Step 5.*

Write a new file `docs/runs/NNNN-<short-slug>.md` (next sequential number, check existing files first) following the structure of the most recent run doc in `docs/runs/`: task, architect's proposed decisions, planner's corrections (with what was verified against real schema/data and why it mattered), whether Step 2.5 triggered and what the user decided, QA findings, and any process lessons for future runs.

**Con `--en`, dos cambios** *(added 2026-09-11)*: (1) el número no se calcula — usa el **run reservado** para esta tarjeta en el plan de ola (`/worktree`, sección "Plan de ola"); si no hay uno, pregunta antes de escribir, porque otra sesión en paralelo puede estar tomando el mismo "siguiente libre". (2) **No toques `INDEX.md`**: la fila la agrega `/worktree sincronizar`, justo antes del PR. Si cada rama paralela la agregara al terminar, todas chocarían en `INDEX.md` en cada merge de develop. Dilo en el reporte final: "fila de INDEX pendiente para `sincronizar`".

Start the file with YAML frontmatter (this repo's `docs/` is used as an Obsidian vault, see `AcmeOrg/Home.md`, en la raíz del vault) with these exact fields: `tipo: run`, `numero: "NNNN"`, `fecha`, `resultado` (PASS/FINDINGS/etc.), `ciclos_qa`, `duracion` (string like "~28 min"), `tokens` (human-readable string like "~196.6K (architect 55.2K + planner 53.3K + coder 39.2K + qa 48.9K)"), `tokens_num` (plain integer, no formatting — the sum in raw tokens, needed for the Dataview SUM query in `Home.md` to work; string-formatted `tokens` alone won't sum), `tokens_fuente` (either `"estimado"` — the normal case, these numbers come from each agent reporting its own usage, not a verified API log — or `"medido"` only if you actually pulled the number from a real usage/billing source; don't write `"medido"` by default), `rama`, `tags` (at least `[run, api-core, orquestador]` plus anything task-specific). Add a wikilink line near the top to the previous run doc and to any ADR/`contexto-negocio/` (raíz del vault) file this run drew on.

Carry QA's `### Runtime/DI Check` section (triggered yes/no, and what it caught if yes) into the run doc's "Hallazgos de QA" — retro tallies this across runs to judge whether the check is worth its cost, so don't drop it even when the answer is "no se activó" or "se activó, no encontró nada nuevo".

Agrega la corrida en **dos** lugares de `docs/runs/INDEX.md`: (1) una linea en la seccion "Mapa de conocimiento", bajo **cada** tema que la corrida haya tocado (una corrida puede aparecer en varios, es a proposito — se busca por tema, no por numero; si no encaja en ninguno, crea el tema); y (2) una fila en el libro mayor con columnas: Fecha, Tarea, Resultado, **Duración**, **Tokens** (same human-readable string as the frontmatter), **Ciclos QA**, Rama, Detalle link. Do this even if the run ended early (escalated to the user after 2 QA cycles, or stopped at the Step 2.5 checkpoint) — record what was tried and why it stopped, that's valuable too.

**Commit en el vault, no en la rama** *(desde 2026-09-16, al mover run docs y ADRs a `agentic-harness`)*: el run doc, la fila de `INDEX.md` (si esta corrida la tocó) y el ADR (si nació uno) viven en la raíz de AcmeOrg, así que el commit del Step 4.5 no los incluye. Comitéalos en `agentic-harness` al cerrar este paso, con el mismo estilo (`docs: registrar corrida NNNN`), y pushea. **Ese checkout lo comparten todas las sesiones abiertas**: nunca `git add -A` ni `git add .`; agrega cada archivo por ruta y revisa su `git diff` antes, porque un archivo que tocaste puede traer además cambios sin commitear de otra sesión (pasó el 2026-09-16: un commit se llevó las ediciones en curso de otra sesión en `Meta/sistema-multiagente-supervisado.md`). Si el diff trae algo que no es tuyo, para y pregunta. El pre-commit corre `scripts/validar-vault.py`: los wikilinks del run doc van con ruta desde la raíz del vault (`[[docs/PREGUNTAS-NEGOCIO]]`, `[[contexto-negocio/...]]`), nunca relativos con `../`.

**ADR check**: before finishing this step, ask yourself whether this run made a real architecture decision (a new dependency/package adopted, a structural pattern change, a new cross-cutting convention) that isn't already covered by an existing file in `docs/adr/`. If so, don't silently let it pass — flag it explicitly in the Final report to the user (below) as "this run made an architecture decision with no ADR — want one written?". Do not draft the ADR yourself unless the user confirms; just surface the gap so it doesn't accumulate unnoticed the way MediatR's adoption and the layered-project split did in early runs, before this check existed. **Si el usuario confirma y se escribe** *(regla del 2026-09-16, al mover los ADRs al vault)*: va en `docs/adr/` con el número reservado en la ola (ver `/worktree`) y nace con `estado: Proposed`, porque la carpeta es compartida y el ADR queda visible para todas las sesiones antes de que el código llegue a `develop`. Pasa a `Accepted` recién cuando mergea el PR que lo implementa. Si ese PR se descarta, el ADR queda en `Proposed` y se avisa al usuario para que decida qué hacer con él.

**Borrador delegado** *(agregado 2026-09-18, ver [`/shunt`](shunt.md))*: confirmado el ADR, el borrador se genera con `code-write.sh --reference <ADR más parecido> --target docs/adr/000N-slug.md --spec "<resumen de la decisión>"` en vez de redactarlo vos mismo. A diferencia del uso normal de `code-write.sh`, acá **sí leés** el archivo generado antes de mostrárselo al usuario — no hay QA/adversary/validador después de un ADR que lo verifique por vos.

## Verificar un hallazgo antes de repetírselo al usuario como hecho
*(added 2026-09-18, corrida piloto GT-<n>)*: el orquestador le repitió al responsable del repo el hallazgo del planner — "hay un bug real hoy en `develop`: el Caso queda huérfano si falla la segunda llamada" — como si ya estuviera comprobado. Era falso: Codex y Antigravity, leyendo el código real por separado, confirmaron que `develop` ya crea el Caso y su interacción en una sola llamada. El planner había comparado contra el worktree viejo (una base deliberadamente anterior a la implementación real) y etiquetó ese estado viejo como "hoy".

Mismo principio que ya existe para el hand-off entre agentes ("el reporte de un agente no es evidencia", Step 3), aplicado al momento exacto donde más cuesta el error — cuando vos, el orquestador, estás a punto de decirle al usuario "esto es un bug real" o "esto ya se confirmó". Antes de esa frase, hacé tu propia verificación mínima: un `grep`/`Read` del archivo citado, o un segundo motor externo (Codex/Antigravity) si el primero ya lo hizo un solo agente. No traslades la conclusión de un solo agente como un hecho comprobado — ni siquiera cuando el agente cita `archivo:línea` con aparente seguridad.

## Final report to the user
Summarize: what was built (files changed), the architect's chosen pattern, any deviations the planner made from the architect's proposal and why, whether the risky-plan checkpoint triggered, the QA verdict, the security review result (Step 4.6), the branch name, whether it was pushed and/or a PR was opened (with the PR URL if so), and any findings that were explicitly deferred (not verifiable, or accepted as known limitations) rather than fixed.

Always include, even when the answer is "none/no":
- **Cambios de agente sin verificar por replay**: if the Step 0 replay-harness check found agent `.md` changes and the user chose not to replay them first, restate that here so it doesn't get lost.
- **Gaps de repo fuente**: if the planner's Risk Flags included the "requiere cambio en repo fuente" flag, restate what was decided (fix at the source later, or accept the limitation) — this is the one flag whose resolution isn't "done" at the end of an api-core run, since fixing it means work in a different repo this pipeline doesn't touch.
